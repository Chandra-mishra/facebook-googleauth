# Node Express Passport Facebook Google Login

This source code is part of [Node Express Passport Facebook Google Login]

Steps to run locally:

* Clone this repo
* run `npm install`
* run `nodemon`
* Open browser then go to `http://127.0.0.1:3000`
